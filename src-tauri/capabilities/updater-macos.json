{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater-macos",
  "description": "macOS-only updater and relaunch access for the main window.",
  "platforms": ["macOS"],
  "windows": ["main"],
  "permissions": [
    "updater:allow-check",
    "updater:allow-download",
    "updater:allow-install",
    "process:allow-restart"
  ]
}
