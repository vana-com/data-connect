# Solutions

Purpose: capture solved problems as reusable knowledge.

## Rules

- Create one solution doc for each feature/bug fix.
- Use `YYMMDD-<slug>.md` naming.
- Include root cause and reusable prevention rule.

## Template

- Start from `YYMMDD-solution-template.md`.
