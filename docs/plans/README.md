# Plans

Purpose: define implementation intent before coding.

## Rules

- Create one plan per non-trivial feature.
- Use `YYMMDD-<slug>.md` naming.
- Include constraints, edge cases, and validation.

## Template

- Start from `YYMMDD-feature-plan-template.md`.
